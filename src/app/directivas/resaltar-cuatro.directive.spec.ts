import { ResaltarCuatroDirective } from './resaltar-cuatro.directive';

describe('ResaltarCuatroDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarCuatroDirective();
    expect(directive).toBeTruthy();
  });
});
