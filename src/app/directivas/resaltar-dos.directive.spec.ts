import { ResaltarDosDirective } from './resaltar-dos.directive';

describe('ResaltarDosDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarDosDirective();
    expect(directive).toBeTruthy();
  });
});
