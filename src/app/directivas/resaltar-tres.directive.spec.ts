import { ResaltarTresDirective } from './resaltar-tres.directive';

describe('ResaltarTresDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarTresDirective();
    expect(directive).toBeTruthy();
  });
});
